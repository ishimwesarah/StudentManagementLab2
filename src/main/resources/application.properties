# Number of threads used for concurrent batch export (option 11).
# This is a fixed, administrator-configured value - end users no
# longer choose this per-request, matching how real production
# systems size shared thread pools.
export.threadPoolSize=6